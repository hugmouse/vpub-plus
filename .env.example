# vpub-plus configuration

# The port the application will listen on inside the container.
PORT=1337
# The port on the host machine to map to the application port.
HOST_PORT=1337

# PostgreSQL connection settings
# The application uses DATABASE_URL to connect to the database.
DATABASE_URL=postgres://vpub:yourpassword@db:5432/vpub?sslmode=disable
POSTGRES_USER=vpub
POSTGRES_PASSWORD=yourpassword
POSTGRES_DB=vpub

# Security settings
# IMPORTANT: Change these to 32-byte random strings
SESSION_KEY=your32byteslongsessionkeyhere
CSRF_KEY=your32byteslongcsrfkeyhere
CSRF_SECURE=true

# Application settings
TITLE=My vpub-plus forum
PROXYING_ENABLED=true

# Database connection pool settings
POSTGRES_MAX_OPEN_CONNECTIONS=0
POSTGRES_MAX_IDLE_CONNECTIONS=0
POSTGRES_MAX_LIFETIME=5m
